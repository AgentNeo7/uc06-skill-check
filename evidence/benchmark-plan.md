# Comparative gate

Status: proposed. Offline fixtures are simulated internal checks.

Freeze owner-reviewed positive, negative and unknown cases before native evaluation. Baseline: configured Salesforce native Apex/Flow validation, native Agentforce evaluations where applicable, and existing Salesforce Code Analyzer/metadata tooling. Candidate: only this module's declared supported subset. Run both against the same metadata version, identities, workload and org API. Record true failures, false alarms, missed cases, runtime cost and maintenance steps. Missing enabled org features or independent labels block comparative claims. No benchmark result from generic Python prototypes establishes a result for this Salesforce rebuild.

Decision: retain as ordinary utility until native comparative evidence demonstrates additional value. If native tooling matches outcomes with lower maintenance, narrow this module or use the native baseline; do not add features to manufacture differentiation.
