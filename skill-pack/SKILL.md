# Synthetic Salesforce review-action qualification task

Version1.0.0. Internal task fixture only.

Given the DX project, inspect ReviewAction and its Flow bindings. Preserve bulk operation, explicit sharing, user-mode data access, consent refusal and correlation uniqueness. Add a test for duplicate request identity. Run Apex tests in an explicitly authorized development org and retain the sf JSON output with the skill version and host version. Do not use production records or report Node checks as native execution.

Qualification compares two distinct recorded native test runs. Passing tests qualify only these assertions, not general Salesforce skill competence. The local examples are synthetic exports.
